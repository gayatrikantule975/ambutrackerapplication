<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/homeMenuMore"
        android:title="My Profile"
        android:icon="@drawable/dots"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/homeMenuMyProfile"
                android:title="My profile"
                app:showAsAction="never"
                android:icon="@drawable/icon_username"
                />
            <item
                android:id="@+id/homeMenuEditProfile"
                android:title="Edit Profile"
                app:showAsAction="never"
                android:icon="@drawable/edit_profile"
                />
            <item
                android:id="@+id/homeMenuHelpline"
                android:title="About Us"
                app:showAsAction="never"
                android:icon="@drawable/icon_phone"
                />
            <item
                android:id="@+id/homeMenuLogout"
                android:title="Logout"
                app:showAsAction="never"
                android:icon="@drawable/icon_logout"
                />
        </menu>
    </item>
</menu>